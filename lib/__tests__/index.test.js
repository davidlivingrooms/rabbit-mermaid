const assert = require("assert");
const rabbitMermaid = require("../index.js");

describe("rabbitMermaid", () => {
  it("has a test", () => {
    assert(rabbitMermaid, "rabbitMermaid should have a test");
  });
});
